[project]
name = "i2stream-backend"
version = "0.1.0"
description = "Hermes WebUI compatibility backend for i2Stream clients"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "httpx>=0.28.1",
    "pydantic-settings>=2.14.1",
    "python-multipart>=0.0.32",
    "uvicorn[standard]>=0.49.0",
]
